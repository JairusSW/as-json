@inline export function serializeInteger<T extends number>(data: T, out: usize, offset: i32): string {
    
}

@inline export function serializeIntegerData<T extends number>(data: T, out: usize, offset: i32): string {
    
}