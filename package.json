{
  "name": "as-json",
  "version": "0.1.0",
  "description": "JSON encoder/decoder for AssemblyScript",
  "types": "assenbly/index.ts",
  "ascMain": "assenbly/index.ts",
  "author": "JairusSW",
  "license": "MIT",
  "scripts": {
    "build:transform": "npx tsc -p ./transform",
    "build:test": "asc assembly/test.ts --transform ./transform/lib/transform.js --target test --explicitStart --exportRuntime",
    "test": "node ./tests/test",
    "bench:build": "asc assembly/bench.ts --transform ./transform/lib/transform.js --runtime minimal --target bench --explicitStart --exportRuntime",
    "bench": "node ./bench/bench"
  },
  "devDependencies": {
    "@assemblyscript/loader": "^0.19.4",
    "@types/line-column": "^1.0.0",
    "as-console": "^5.0.3",
    "assemblyscript": "^0.19.4",
    "typescript": "^4.3.4"
  },
  "dependencies": {
    "as-string-sink": "^0.1.1",
    "line-column": "^1.0.2",
    "visitor-as": "^0.6.0"
  }
}
