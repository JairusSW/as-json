{
  "name": "as-kati",
  "version": "0.0.3",
  "description": "The God of all JSON. Extremely fast JSON serialize and deserialize.",
  "types": "assenbly/index.ts",
  "ascMain": "assenbly/index.ts",
  "author": "JairusSW",
  "license": "MIT",
  "devDependencies": {
    "@assemblyscript/loader": "^0.18.31",
    "@types/line-column": "^1.0.0",
    "as-console": "^5.0.3",
    "assemblyscript": "^0.18.31"
  },
  "dependencies": {
    "as-inliner": "^0.0.3",
    "as-string-sink": "^0.1.1",
    "line-column": "^1.0.2",
    "visitor-as": "^0.6.0",
    "@assemblyscript/loader": "^0.18.32"
  },
  "scripts": {
    "asbuild:untouched": "asc assembly/index.ts --target debug --transform ./transform/lib/transform.js --explicitStart",
    "asbuild:optimized": "asc assembly/index.ts --target release --runtime stub --transform ./transform/lib/transform.js --explicitStart",
    "asbuild": "yarn asbuild:untouched && yarn asbuild:optimized",
    "test": "node tests"
  }
}